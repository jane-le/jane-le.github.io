import React, { Component } from "react"

class Cloud extends Component {
    render() {
        const {
            fill,
        } = this.props;
        return(
        );
    }
}

export default Cloud
